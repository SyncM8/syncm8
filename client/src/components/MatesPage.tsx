import React, { Component } from "react";
import { Typography } from "antd";

const { Title } = Typography;

export default class MatesPage extends Component {
  render(): React.ReactNode {
    return (
      <>
        <Title>Mates!</Title>
      </>
    );
  }
}
